continue
 @diff_percentage = distance / params[:left_section].length * 100
 params[:left_section].length
distance
continue
Diffy::Diff.new(params[:left_section], params[:right_section]).to_s(:html_simple)
continue
exit
continue
    end
      whitelisted[:navbar] = params[:page][:navbar].join(',')
params["navbar"]    params.require(:page).permit(:header, :footer, :navbar).tap do |whitelisted|
params["navbar"]
params[:navbar]
params[:navbar].join(',')
params
